function power(under,on) {
    var value;
    for (var index = 1;index<on;index++) {
        value = under*under;
    }
    return under;
}

function sqrt(x) {
    return Math.sqrt(x);
}

function lg(x) {
    return Math.log10(x);
}

// This model is making at 2024/5/2 20:54
// This model can make log10 power sqrt
